class Flowfield {
  constructor(options) {
    
  }
  // Samples the field at the given point, interpolating if necessary
  sample(point) {
    const value = Math.random() * Math.PI * 2;
    return value;
  }

}
